{{- if and .Values.redis.enabled .Values.redis.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: auth-redis-netpol
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "auth-service.redis.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "auth-service.redis.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  {{- toYaml .Values.redis.networkPolicy.ingress | nindent 2 }}
  egress:
  - to: []
    ports:
    - protocol: UDP
      port: 53
{{- end }}
