╔══════════════════════════════════════════════════════════════════════════════╗
║                    IQ Scaffold Bookstore Service Deployed                        ║
╚══════════════════════════════════════════════════════════════════════════════╝

Release Name:    {{ .Release.Name }}
Namespace:       {{ .Values.namespace.name }}
Environment:     {{ .Values.global.environment }}
Chart Version:   {{ .Chart.Version }}
App Version:     {{ .Chart.AppVersion }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📦 DEPLOYED COMPONENTS

  ✓ Bookstore Service ({{ .Values.replicaCount }} replicas)
  {{- if .Values.postgresql.enabled }}
  ✓ PostgreSQL {{ .Values.postgresql.image.tag }} ({{ .Values.postgresql.persistence.size }} storage)
  {{- end }}
  {{- if .Values.redis.enabled }}
  ✓ Redis {{ .Values.redis.image.tag }} ({{ .Values.redis.persistence.size }} storage)
  {{- end }}
  {{- if .Values.autoscaling.enabled }}
  ✓ Horizontal Pod Autoscaler ({{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }} pods)
  {{- end }}
  {{- if .Values.networkPolicy.enabled }}
  ✓ Network Policies
  {{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 VERIFY DEPLOYMENT

  # Check pod status
  kubectl get pods -n {{ .Values.namespace.name }} -l app.kubernetes.io/name=iqscaffold-bookstore-service

  # Watch deployment rollout
  kubectl rollout status deployment/{{ include "bookstore-service.fullname" . }} -n {{ .Values.namespace.name }}

  # Check all resources
  kubectl get all -n {{ .Values.namespace.name }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 ACCESS THE SERVICE

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}

  API Endpoint:     http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}/api/v1/bookstore
  Swagger UI:       http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}/swagger-ui.html
  API Docs:         http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}/api-docs
  Health Check:     http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}/actuator/health
  Metrics:          http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}/actuator/prometheus
{{- end }}
{{- else }}

  # Port forward to access locally
  kubectl port-forward -n {{ .Values.namespace.name }} svc/{{ include "bookstore-service.fullname" . }} 8080:8080

  API Endpoint:     http://localhost:8080/api/v1/bookstore
  Swagger UI:       http://localhost:8080/swagger-ui.html
  API Docs:         http://localhost:8080/api-docs
  Health Check:     http://localhost:8080/actuator/health
  Metrics:          http://localhost:8080/actuator/prometheus
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🏥 HEALTH CHECKS

  # Liveness probe
  kubectl exec -n {{ .Values.namespace.name }} \
    $(kubectl get pod -n {{ .Values.namespace.name }} -l app.kubernetes.io/name=iqscaffold-bookstore-service -o jsonpath='{.items[0].metadata.name}') \
    -- curl -s http://localhost:8080/actuator/health/liveness

  # Readiness probe
  kubectl exec -n {{ .Values.namespace.name }} \
    $(kubectl get pod -n {{ .Values.namespace.name }} -l app.kubernetes.io/name=iqscaffold-bookstore-service -o jsonpath='{.items[0].metadata.name}') \
    -- curl -s http://localhost:8080/actuator/health/readiness

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 MONITORING

  # View logs
  kubectl logs -f -n {{ .Values.namespace.name }} -l app.kubernetes.io/name=iqscaffold-bookstore-service

  # View metrics
  kubectl port-forward -n {{ .Values.namespace.name }} svc/{{ include "bookstore-service.fullname" . }} 8080:8080
  curl http://localhost:8080/actuator/prometheus

  {{- if .Values.autoscaling.enabled }}
  # Check autoscaling
  kubectl get hpa -n {{ .Values.namespace.name }}
  {{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🗄️  DATABASE ACCESS

  {{- if .Values.postgresql.enabled }}
  # Connect to PostgreSQL
  kubectl exec -it -n {{ .Values.namespace.name }} \
    $(kubectl get pod -n {{ .Values.namespace.name }} -l app.kubernetes.io/name=bookstore-postgres -o jsonpath='{.items[0].metadata.name}') \
    -- psql -U iqscaffold_user -d {{ .Values.postgresql.env.POSTGRES_DB }}
  {{- end }}

  {{- if .Values.redis.enabled }}
  # Connect to Redis
  kubectl exec -it -n {{ .Values.namespace.name }} \
    $(kubectl get pod -n {{ .Values.namespace.name }} -l app.kubernetes.io/name=bookstore-redis -o jsonpath='{.items[0].metadata.name}') \
    -- redis-cli
  {{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION

  Helm Chart:       helm/bookstore-service/README.md
  Deployment Guide: helm/bookstore-service/DEPLOYMENT.md
  Service README:   iqscaffold-bookstore-service/README.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚙️  NEXT STEPS

  {{- if eq .Values.global.environment "local" }}
  1. Verify all pods are running
  2. Test API endpoints via Swagger UI
  3. Check database migrations completed
  4. Test book catalog and inventory operations
  {{- else if eq .Values.global.environment "staging" }}
  1. Verify deployment health
  2. Run smoke tests
  3. Check observability dashboards
  4. Validate JWT authentication with User Service
  5. Test integration with Gateway Service
  {{- else if eq .Values.global.environment "production" }}
  1. Verify all pods are healthy and distributed
  2. Check HPA is functioning
  3. Validate monitoring and alerting
  4. Verify backup jobs are scheduled
  5. Test failover scenarios
  6. Review security policies
  {{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 USEFUL COMMANDS

  # Get release info
  helm status {{ .Release.Name }} -n {{ .Values.namespace.name }}
  helm get values {{ .Release.Name }} -n {{ .Values.namespace.name }}

  # Upgrade release
  helm upgrade {{ .Release.Name }} ./helm/bookstore-service -n {{ .Values.namespace.name }}

  # Rollback release
  helm rollback {{ .Release.Name }} -n {{ .Values.namespace.name }}

  # Uninstall release
  helm uninstall {{ .Release.Name }} -n {{ .Values.namespace.name }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{- if eq .Values.global.environment "local" }}
⚠️  LOCAL DEVELOPMENT NOTICE

This deployment uses embedded secrets suitable for local development only.
For staging/production, use external secret management (AWS Secrets Manager, Vault).
{{- end }}

{{- if not .Values.postgresql.persistence.enabled }}
⚠️  WARNING: PostgreSQL persistence is disabled. Data will be lost on pod restart!
{{- end }}

{{- if not .Values.redis.persistence.enabled }}
⚠️  WARNING: Redis persistence is disabled. Cache will be lost on pod restart!
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For support: platform@iqscaffold.site
Documentation: https://docs.iqscaffold.site
