Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

{{- if .Values.ingress.enabled }}

The application is accessible via the following URL(s):
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- end }}

Service Information:
  - Service Name: {{ include "user-service.fullname" . }}
  - Namespace: {{ .Values.namespace.name }}
  - Port: {{ .Values.service.port }}

{{- if .Values.postgresql.enabled }}
  - PostgreSQL: user-postgres:{{ .Values.postgresql.service.port }}
{{- end }}
{{- if .Values.redis.enabled }}
  - Redis: user-redis:{{ .Values.redis.service.port }}
{{- end }}

To get the application logs:
  $ kubectl logs -f -n {{ .Values.namespace.name }} -l app.kubernetes.io/name=gripday-user-service

To access the application directly using port-forward:
  $ kubectl port-forward -n {{ .Values.namespace.name }} svc/{{ include "user-service.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

Then open your browser to:
  http://localhost:{{ .Values.service.port }}

API Documentation (Swagger UI):
  http://localhost:{{ .Values.service.port }}/swagger-ui.html

Health Check:
  http://localhost:{{ .Values.service.port }}/actuator/health
