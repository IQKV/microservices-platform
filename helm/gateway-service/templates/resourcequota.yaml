{{- if .Values.resourceQuota.enabled }}
apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ include "gateway-service.fullname" . }}-quota
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "gateway-service.labels" . | nindent 4 }}
spec:
  hard:
    {{- toYaml .Values.resourceQuota.hard | nindent 4 }}
{{- end }}
