# Test environment values for gateway-service

global:
  environment: test
  platform: gripday

namespace:
  name: gripday-test-env
  labels:
    app.kubernetes.io/component: gateway
    environment: test

replicaCount: 3

image:
  repository: gripday/gateway-service
  pullPolicy: Always
  tag: "test-latest"

podAnnotations:
  prometheus.io/scrape: "true"
  prometheus.io/port: "8080"
  prometheus.io/path: "/actuator/prometheus"
  environment: "test"

priorityClassName: high-priority

ingress:
  enabled: true
  className: nginx
  annotations:
    # cert-manager.io/cluster-issuer: letsencrypt-staging  # Uncomment if using cert-manager
    nginx.ingress.kubernetes.io/ssl-redirect: "false" # TLS optional - set to "true" if TLS enabled
    nginx.ingress.kubernetes.io/rate-limit: "500"
    nginx.ingress.kubernetes.io/cors-allow-origin: "https://gripday.website, https://*.gripday.website"
    nginx.ingress.kubernetes.io/proxy-body-size: "20m"
  hosts:
    - host: api.gripday.website
      paths:
        - path: /
          pathType: Prefix
  tls: [] # TLS disabled by default - configure externally if needed
  # Uncomment to enable TLS:
  # tls:
  #   - secretName: gateway-test-tls
  #     hosts:
  #       - api.gripday.website

resources:
  requests:
    memory: "1Gi"
    cpu: "750m"
  limits:
    memory: "2Gi"
    cpu: "1500m"

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 15
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

podDisruptionBudget:
  enabled: true
  minAvailable: 2

env:
  - name: SPRING_PROFILES_ACTIVE
    value: "test"
  - name: GRIPDAY_CACHE_REDIS_HOST
    value: "gateway-redis"
  - name: GRIPDAY_CACHE_REDIS_PORT
    value: "6379"
  - name: GRIPDAY_CACHE_REDIS_DATABASE
    value: "1"
  - name: USER_SERVICE_JWK_URI
    value: "http://user-service/.well-known/jwks.json"
  - name: GRIPDAY_GATEWAY_ROUTING_SERVICES_USER_SERVICE_URI
    value: "http://user-service"
  - name: GRIPDAY_GATEWAY_ROUTING_SERVICES_BOOKSTORE_SERVICE_URI
    value: "http://bookstore-service"
  - name: GRIPDAY_GATEWAY_CORS_ALLOWED_ORIGINS
    value: "https://gripday.website,https://*.gripday.website"
  - name: GRIPDAY_OBSERVABILITY_TRACING_ENDPOINT
    value: "http://jaeger-collector.observability:14268/api/traces"
  - name: OTEL_EXPORTER_OTLP_ENDPOINT
    value: "http://otel-collector.observability:4317"
  - name: OTEL_SERVICE_NAME
    value: "gripday-gateway-service"
  - name: OTEL_RESOURCE_ATTRIBUTES
    value: "service.name=gripday-gateway-service,service.version=test,deployment.environment=test"
  - name: JAVA_OPTS
    value: "-Xms768m -Xmx1536m -XX:+UseG1GC -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/tmp/heapdump.hprof"

secretEnv:
  - name: GRIPDAY_CACHE_REDIS_PASSWORD
    secretKeyRef:
      name: gateway-service-secrets
      key: GRIPDAY_CACHE_REDIS_PASSWORD
      optional: true
  - name: GRIPDAY_AUTH_JWT_SECRET
    secretKeyRef:
      name: gateway-service-secrets
      key: GRIPDAY_AUTH_JWT_SECRET

secrets:
  enabled: true
  data: {} # Secrets should be managed externally in test

configMap:
  enabled: true
  data:
    application.yml: |
      spring:
        profiles:
          active: test

      logging:
        level:
          org.gripday.gateway: INFO
          org.springframework.cloud.gateway: INFO
          org.springframework.security: WARN
          AUDIT: INFO
        pattern:
          console: '{"timestamp":"%d{yyyy-MM-dd HH:mm:ss.SSS}","level":"%level","thread":"%thread","correlationId":"%X{correlationId}","userId":"%X{userId}","username":"%X{username}","logger":"%logger{36}","message":"%msg"}%n'

redis:
  enabled: true
  image:
    tag: "7.2-alpine"
  replicaCount: 1
  persistence:
    enabled: true
    size: 5Gi
    storageClass: standard
  config:
    maxmemory: "768mb"
    maxmemoryPolicy: "allkeys-lru"
    appendonly: "yes"
  resources:
    requests:
      memory: "384Mi"
      cpu: "300m"
    limits:
      memory: "768Mi"
      cpu: "600m"

networkPolicy:
  enabled: true

resourceQuota:
  enabled: true

limitRange:
  enabled: true
