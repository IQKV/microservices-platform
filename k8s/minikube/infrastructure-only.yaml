# Gripday Platform - Infrastructure Only
# Deploy just databases and Redis for local development
# Useful when running services locally but want databases in Kubernetes

---
# Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: gripday
  labels:
    name: gripday
    environment: local

---
# ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: gripday-infra-config
  namespace: gripday-dev-env
data:
  REDIS_PORT: "6379"

---
# Secrets
apiVersion: v1
kind: Secret
metadata:
  name: gripday-infra-secrets
  namespace: gripday-dev-env
type: Opaque
data:
  # gripday_user
  DB_USERNAME: Z3JpcGRheV91c2Vy
  # gripday_password
  DB_PASSWORD: Z3JpcGRheV9wYXNzd29yZA==

---
# PostgreSQL for User Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-auth
  namespace: gripday-dev-env
  labels:
    app: postgres-auth
    component: database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-auth
  template:
    metadata:
      labels:
        app: postgres-auth
        component: database
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: gripday_auth
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: gripday-infra-secrets
                  key: DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gripday-infra-secrets
                  key: DB_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - gripday_user
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - gripday_user
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: postgres-data
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: postgres-auth
  namespace: gripday-dev-env
  labels:
    app: postgres-auth
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30432
  selector:
    app: postgres-auth

---
# PostgreSQL for Bookstore Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-bookstore
  namespace: gripday-dev-env
  labels:
    app: postgres-bookstore
    component: database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-bookstore
  template:
    metadata:
      labels:
        app: postgres-bookstore
        component: database
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: gripday_bookstore
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: gripday-infra-secrets
                  key: DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gripday-infra-secrets
                  key: DB_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - gripday_user
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - gripday_user
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: postgres-data
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: postgres-bookstore
  namespace: gripday-dev-env
  labels:
    app: postgres-bookstore
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30433
  selector:
    app: postgres-bookstore

---
# Redis
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: gripday-dev-env
  labels:
    app: redis
    component: cache
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
        component: cache
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          command:
            - redis-server
            - --appendonly
            - "yes"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          volumeMounts:
            - name: redis-data
              mountPath: /data
          livenessProbe:
            exec:
              command:
                - redis-cli
                - ping
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - redis-cli
                - ping
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: redis-data
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: gripday-dev-env
  labels:
    app: redis
spec:
  type: NodePort
  ports:
    - port: 6379
      targetPort: 6379
      nodePort: 30379
  selector:
    app: redis
