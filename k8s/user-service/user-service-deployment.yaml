apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: gripday-dev-env
  labels:
    app.kubernetes.io/name: gripday-user-service
    app.kubernetes.io/component: authentication
    app.kubernetes.io/part-of: gripday
    app.kubernetes.io/version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: gripday-user-service
      app.kubernetes.io/component: authentication
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gripday-user-service
        app.kubernetes.io/component: authentication
        app.kubernetes.io/part-of: gripday
        app.kubernetes.io/version: "1.0.0"
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: user-service-sa
      priorityClassName: high-priority
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: user-service
          image: gripday/user-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          env:
            # Spring Profile
            - name: SPRING_PROFILES_ACTIVE
              value: "local"

            # Database Configuration
            - name: GRIPDAY_DATABASE_URL
              value: "jdbc:postgresql://user-postgres:5432/gripday_user_local"
            - name: GRIPDAY_DATABASE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: user-service-secrets
                  key: GRIPDAY_DATABASE_USERNAME
            - name: GRIPDAY_DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: user-service-secrets
                  key: GRIPDAY_DATABASE_PASSWORD

            # Redis Configuration
            - name: GRIPDAY_CACHE_REDIS_HOST
              value: "user-redis"
            - name: GRIPDAY_CACHE_REDIS_PORT
              value: "6379"
            - name: GRIPDAY_CACHE_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: user-service-secrets
                  key: GRIPDAY_CACHE_REDIS_PASSWORD
                  optional: true
            - name: GRIPDAY_CACHE_REDIS_DATABASE
              value: "0"

            # JWT Configuration
            - name: GRIPDAY_AUTH_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: user-service-secrets
                  key: GRIPDAY_AUTH_JWT_SECRET

            # OAuth2 Configuration
            - name: GRIPDAY_AUTH_OAUTH2_GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: user-service-secrets
                  key: GRIPDAY_AUTH_OAUTH2_GOOGLE_CLIENT_ID
            - name: GRIPDAY_AUTH_OAUTH2_GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: user-service-secrets
                  key: GRIPDAY_AUTH_OAUTH2_GOOGLE_CLIENT_SECRET
            - name: GRIPDAY_AUTH_OAUTH2_GOOGLE_ENABLED
              value: "false"

            # Observability Configuration
            - name: GRIPDAY_OBSERVABILITY_TRACING_ENDPOINT
              value: "http://jaeger-collector:14268/api/traces"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector:4317"
            - name: OTEL_SERVICE_NAME
              value: "gripday-user-service"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.name=gripday-user-service,service.version=1.0.0,deployment.environment=local"

            # JVM Configuration
            - name: JAVA_OPTS
              value: "-Xms256m -Xmx512m -XX:+UseG1GC -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0"

          volumeMounts:
            - name: config-volume
              mountPath: /app/config
              readOnly: true
            - name: tmp-volume
              mountPath: /tmp

          resources:
            requests:
              memory: "384Mi"
              cpu: "250m"
            limits:
              memory: "768Mi"
              cpu: "500m"

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
            successThreshold: 1

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1

          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 12
            successThreshold: 1

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1001
            capabilities:
              drop:
                - ALL

          lifecycle:
            preStop:
              exec:
                command:
                  - sh
                  - -c
                  - sleep 15

      volumes:
        - name: config-volume
          configMap:
            name: user-service-config
        - name: tmp-volume
          emptyDir: {}

      restartPolicy: Always
      terminationGracePeriodSeconds: 30

      # Topology spread constraints for better distribution
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: gripday-user-service
              app.kubernetes.io/component: authentication
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: gripday-user-service
              app.kubernetes.io/component: authentication

      # Pod disruption budget for high availability
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - gripday-user-service
                topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: user-service-sa
  namespace: gripday-dev-env
  labels:
    app.kubernetes.io/name: gripday-user-service
    app.kubernetes.io/component: authentication
    app.kubernetes.io/part-of: gripday
automountServiceAccountToken: false
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: user-service-pdb
  namespace: gripday-dev-env
  labels:
    app.kubernetes.io/name: gripday-user-service
    app.kubernetes.io/component: authentication
    app.kubernetes.io/part-of: gripday
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gripday-user-service
      app.kubernetes.io/component: authentication
