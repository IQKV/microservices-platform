spring:
  cloud:
    gateway:
      routes:
        - id: test-auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/v1/auth/**

logging:
  level:
    org.gripday: DEBUG