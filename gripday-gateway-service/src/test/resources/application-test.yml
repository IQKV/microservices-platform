spring:
  cloud:
    gateway:
      routes:
        - id: test-user-service
          uri: http://localhost:8080
          predicates:
            - Path=/api/v1/auth/**

logging:
  level:
    org.gripday: DEBUG