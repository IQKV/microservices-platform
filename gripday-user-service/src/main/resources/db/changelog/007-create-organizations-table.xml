<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <changeSet id="007-create-organizations" author="gripday">
        <comment>Create organizations table with OneToOne relationship to users</comment>

        <createTable tableName="organizations">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="description" type="VARCHAR(1000)">
                <constraints nullable="true"/>
            </column>

            <column name="industry" type="VARCHAR(100)">
                <constraints nullable="true"/>
            </column>

            <column name="website" type="VARCHAR(255)">
                <constraints nullable="true"/>
            </column>

            <column name="phone" type="VARCHAR(50)">
                <constraints nullable="true"/>
            </column>

            <column name="address" type="VARCHAR(500)">
                <constraints nullable="true"/>
            </column>

            <column name="city" type="VARCHAR(100)">
                <constraints nullable="true"/>
            </column>

            <column name="country" type="VARCHAR(100)">
                <constraints nullable="true"/>
            </column>

            <column name="enabled" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>

            <column name="tenant_id" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <rollback>
            <dropTable tableName="organizations"/>
        </rollback>
    </changeSet>

    <changeSet id="007.1-create-organizations" author="gripday">
        <comment>Add organization_id column to users table for OneToOne relationship</comment>

        <addColumn tableName="users">
            <column name="organization_id" type="BIGINT">
                <constraints nullable="true"/>
            </column>
        </addColumn>

        <addForeignKeyConstraint
                baseTableName="users"
                baseColumnNames="organization_id"
                constraintName="fk_users_organization"
                referencedTableName="organizations"
                referencedColumnNames="id"
                onDelete="SET NULL"
                onUpdate="CASCADE"/>

        <rollback>
            <dropForeignKeyConstraint baseTableName="users" constraintName="fk_users_organization"/>
            <dropColumn tableName="users" columnName="organization_id"/>
        </rollback>
    </changeSet>

    <changeSet id="007.2-create-organizations" author="gripday">
        <comment>Add indexes for organizations table</comment>

        <createIndex indexName="idx_organizations_tenant_id" tableName="organizations">
            <column name="tenant_id"/>
        </createIndex>

        <createIndex indexName="idx_organizations_name" tableName="organizations">
            <column name="name"/>
        </createIndex>

        <createIndex indexName="idx_organizations_enabled" tableName="organizations">
            <column name="enabled"/>
        </createIndex>

        <createIndex indexName="idx_organizations_created_at" tableName="organizations">
            <column name="created_at"/>
        </createIndex>

        <createIndex indexName="idx_users_organization_id" tableName="users">
            <column name="organization_id"/>
        </createIndex>

        <rollback>
            <dropIndex indexName="idx_organizations_tenant_id" tableName="organizations"/>
            <dropIndex indexName="idx_organizations_name" tableName="organizations"/>
            <dropIndex indexName="idx_organizations_enabled" tableName="organizations"/>
            <dropIndex indexName="idx_organizations_created_at" tableName="organizations"/>
            <dropIndex indexName="idx_users_organization_id" tableName="users"/>
        </rollback>
    </changeSet>

    <changeSet id="007.3-create-organizations" author="gripday">
        <comment>Enforce per-tenant organization name uniqueness and one-to-one user organization relation</comment>

        <addUniqueConstraint 
                tableName="organizations" 
                columnNames="tenant_id, name" 
                constraintName="uq_organizations_tenant_name"/>

        <addUniqueConstraint 
                tableName="users" 
                columnNames="organization_id" 
                constraintName="uq_users_organization_id"/>

        <rollback>
            <dropUniqueConstraint tableName="organizations" constraintName="uq_organizations_tenant_name"/>
            <dropUniqueConstraint tableName="users" constraintName="uq_users_organization_id"/>
        </rollback>
    </changeSet>
</databaseChangeLog>
